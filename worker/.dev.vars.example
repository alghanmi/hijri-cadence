# Copy to `.dev.vars` (gitignored) for local `wrangler dev`. Both plain vars
# and secrets are loaded from the same file by wrangler dev.
#
# In production the deploy passes these with `wrangler deploy --var`; this
# file only exists for local `wrangler dev`.

INSTANCE_ID=local
LOG_LEVEL=debug
HEARTBEAT_URL=
